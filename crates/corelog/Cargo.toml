[package]
name = "corelog"
version = "0.1.0"
edition = "2024"

[dependencies]
core_lib = { path = "../core_lib" }
clap = { version = "4.4", features = ["derive"] }
chrono = "0.4"
colored = "2.0"
anyhow = "1.0"
directories = "5.0"
