# Hyprcore Fragments
- **Brave**: Generates a `manifest.json` theme extension.
- **Stylus**: Generates a UserCSS file and a JSON import file.
- **Hyprcore Internals**: Added `hex_to_rgb` filter to Tera engine in `main.rs`.
