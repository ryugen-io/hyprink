# Corelog
- **Rich Formatting**: Messages support inline tags like `<bold>`, `<primary>`, `<error>`, etc.
- **Strict Theming**: Tags *must* match keys in `theme.toml`. Unknown tags render as plain text. No ANSI fallbacks.
- **Demo**: `just show` runs `assets/scripts/demo_logs.sh` to demonstrate capabilities.
